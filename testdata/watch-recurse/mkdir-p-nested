# Sequential mkdir under a recursive watch should emit CREATE for each dir.
require recurse

watch /...

mkdir -p /a/b/c
await-recurse /a/b/c
touch /a/b/c/file

Output:
	create   /a
	create   /a/b
	create   /a/b/c
	create   /a/b/c/file
